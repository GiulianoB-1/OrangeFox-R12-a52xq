# OrangeFox a52xq unified microSD pre-mount helper v2
# Runs before the recovery UI starts. Failure is non-fatal.
on boot
    exec u:r:recovery:s0 root root -- /system/bin/sh /sbin/of_sdcard_mount.sh
